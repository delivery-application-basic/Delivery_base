/**
 * CustomMarker - Custom map marker (optional wrapper)
 */

import React from 'react';
import { Marker } from 'react-native-maps';

export const CustomMarker = (props) => <Marker {...props} />;

export default CustomMarker;
